{
  "version": 2,
  "builds": [
    {
      "src": "project.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "nx build server --prod",
        "outputDirectory": "dist/apps/server"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/apps/server/main.js"
    }
  ]
}
